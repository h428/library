
Book 先简单考虑不按实例编号，只按一类书编号，统计数量，不考虑实例，即不记录借出具体的哪一本书，所有的同名书都是一样的

Borrow 借书记录，一次可以借多本书，有多个 BorrowItem，每个 BorrowItem 记录一本书的借出情况

BorrowItem 借书记录项，借书记录中的一本书的借出情况，记录了借出的书的实例编号，借出时间，归还时间，是否归还，是否超期，超期天数，超期罚款，允许单本归还，允许单本续借

